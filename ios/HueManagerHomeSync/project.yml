name: HueManagerHomeSync
options:
  bundleIdPrefix: com.albaintor
  deploymentTarget:
    iOS: "18.0"
settings:
  base:
    SWIFT_VERSION: 5.10
targets:
  HueManagerHomeSync:
    type: application
    platform: iOS
    sources:
      - Sources
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.albaintor.HueManagerHomeSync
        CODE_SIGN_ENTITLEMENTS: HueManagerHomeSync.entitlements
        INFOPLIST_FILE: Info.plist
        TARGETED_DEVICE_FAMILY: "1,2"
        MARKETING_VERSION: "0.7.0"
        CURRENT_PROJECT_VERSION: "1"
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
    entitlements:
      path: HueManagerHomeSync.entitlements
schemes:
  HueManagerHomeSync:
    build:
      targets:
        HueManagerHomeSync: all
    run:
      config: Debug
    archive:
      config: Release
